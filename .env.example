# Origin of the pilatix_backend API - scheme, host and port only.
# Do NOT include /api/v1: the version lives in src/api/endpoints.ts so that
# endpoints can move to a new version independently.
VITE_API_BASE_URL=http://localhost:8080
